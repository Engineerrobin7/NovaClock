# Build Instructions

The `build.gradle.kts` file has been corrected.

To build the APK:

1.  Navigate to the project root directory: `c:\Users\ROBIN\OneDrive\Desktop\nova_clock`
2.  Run the command: `flutter build apk --release`
